name: Check for push
on:
  push:
    branches: [ main ]


jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      
      - name: script
        run: |
             echo "Hey this is 2nd workflow triggered by repoA"
